# Artifact Set Model

An artifact set is a product-neutral collection of product-associated files.
Factorio modsets are represented through the Factorio binding as artifact-set
semantics, not as a universal `modset` concept.
