# Universal Launcher Kernel Source

Universal launcher owns products, install references, instances, profiles,
account references, artifact sets, launch plans, command graph, diagnostics,
dry-run, and audit.

Public launcher kernel ABI lives under `include/ulk/`. Public launcher utility,
UI model, command graph, and platform-facing ABI lives under `include/ulu/`.
Private implementation belongs here.
