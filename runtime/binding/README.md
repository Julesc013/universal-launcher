# Binding Host

Product bindings plug into the universal launcher through a C ABI with an
experimental correctness floor. Compatible extension or an explicitly
versioned descriptor is required when metadata grows. The binding host must not
import product-specific rules into the launcher kernel.
