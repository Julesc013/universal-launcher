# Win32 GUI

Optional Windows launcher GUI shell. A concrete implementation can use
WinForms, but this directory is named for the Windows provider boundary rather
than a specific toolkit.

It must call the same command graph and must not own launcher logic.
