command_id = "install_refs.inspect"
owner = "universal-launcher"
effects = ["workspace_read"]
request_schema = "contracts/schema/command/command_request.v1.schema.json"
response_schema = "contracts/schema/command/command_response.v1.schema.json"
result_schema = "contracts/result/result.v1.schema.json"
refusal_schema = "contracts/refusal/refusal.v1.schema.json"
dry_run_behavior = "Reads one registered install reference through the selected product binding."
refusal_codes = ["invalid_argument", "unknown_install", "product_binding_required"]
cli_mapping_later = "facman installs inspect <id> --json"
